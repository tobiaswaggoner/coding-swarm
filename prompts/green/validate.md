## Aufgabe: Build/Test Validierung (Validate-Task)

### Branch
`{{BRANCH}}`

### Prüfungen
1. Checke den Branch aus
2. Installiere Dependencies: `npm install` (falls package.json vorhanden)
3. Führe Build aus: `npm run build` (falls vorhanden)
4. Führe Tests aus: `npm test` (falls vorhanden)
5. Führe Linting aus: `npm run lint` (falls vorhanden)

### Ergebnis melden
- Build-Status (success/failed)
- Test-Status (success/failed/skipped)
- Lint-Status (success/failed/skipped)
- Fehlermeldungen falls vorhanden

WICHTIG: Keine Änderungen am Code vornehmen. Nur validieren und berichten.
